name: "L4 // CORRUPTED LOG"
author: "IEEE RAS · NODE ZERO"
category: "STAGE 4"
value: 400
type: standard
state: visible

attachments:
  - corrupted.log

description: |
  > The recovery log handed you a scrambled line. It's not damaged — it's
  > *enciphered*.

  Take the garbled log text and apply a classical cipher shift to it.

  > **Shift: `--...`**

  Decoded, the plaintext names the next artifact to chase.

  [⬇ Download corrupted.log] (attach via Admin → Challenge → Files)

  ---
  Skills: classical ciphers (Caesar / Vigenère), Morse code.
  Flag format: `nodezero{...}`  •  Stage 4 of 10.

# ORGANIZER: Caesar over the log text; shift = 7, given in the description as
# Morse code (--...) instead of requiring recall from Stage 3's package.xml
# version. Decoded plaintext ends by naming the mission_047 rosbag for Stage 5
# (deliberately does not name the topic -- that's Stage 5's own puzzle).
# NOTE: Stage 3's recovery log still tells players the shift equals its own
# package version (7) as a HANDOFF -- that's now a redundant/confirming path,
# not the only one. Leave as-is unless you want that line reworded too.
flags:
  - nodezero{cipher_one_broken}

hints:
  - content: "The bolded line in the description isn't decoration — it's Morse code. Decode the dots and dashes to a single digit first."
    cost: 25
  - content: "That digit is your Caesar shift. Apply it across the alphabet with `caesar`/`tr` or a 3-line Python loop. The plaintext names a rosbag file — write it down."
    cost: 50

requirements:
  - "L3 // COLD START"
